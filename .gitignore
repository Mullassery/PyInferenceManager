# Sensitive files and credentials
.env
.env.local
.env.*.local
*.key
*.pem
*.crt
credentials.json
secrets.json
.credentials
config.local.*
.config.local
oauth_tokens.json

# IDE files
.vscode/
.idea/
*.swp
*~
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/

# Compiled PyO3 extension module (built locally via `maturin develop`/`maturin
# build` into pyinferencemanager/ — never hand-committed; a stale copy of
# this previously ended up checked into git under a dead src/ layout).
pyinferencemanager/_core*.so
pyinferencemanager/_core*.pyd

# Editor/manual backup files (a stale README.md.bak and __init__.py.bak
# previously shipped in the repo and silently contradicted the real files).
*.bak

# Virtual environments
venv/
env/
.venv

# Rust
target/
Cargo.lock

# Temp files
*.tmp
*.log
temp/
tmp/
